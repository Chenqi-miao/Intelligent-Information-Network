# CESNET-TimeSeries24 数据集内容说明

> **论文**: CESNET-TimeSeries24: Time Series Dataset for Network Traffic Anomaly Detection and Forecasting
> **作者**: Josef Koumar, Karel Hynek, Tomáš Čejka, Pavel Šiška (CESNET & CTU Prague)
> **发布**: Nature Scientific Data, 2025 | Zenodo DOI: [10.5281/zenodo.13382427](https://zenodo.org/records/13382427)
> **本作业使用**: ISP 流量预测（复现主论文 "Comparative Analysis of Deep Learning Models...")

> ⚠️ **数据完整性说明**：`institution_subnets/` 从 Zenodo 下载时不完整（下载中断），当前有 546/548 条序列，差 2 条。不影响主实验（机构级 283 条完整），若需完整子网数据请重新从 Zenodo 下载完整 tar.gz 后解压覆盖。

---

## 一、数据集总览

| 项目 | 内容 |
|------|------|
| **数据来源** | CESNET3 网络 — 捷克国家级科研教育 ISP |
| **时间跨度** | 2023-10-09 至 2024-07-14（40 周） |
| **覆盖范围** | ~275,000 个活跃 IP 地址 |
| **原始数据量** | 660 亿 IP 流（4 万亿包, ~3.7 PB） |
| **匿名化** | 使用数据库 ID 替代 IP / 机构 / 子网标识 |
| **用途** | 流量预测 + 异常检测 |
| **授权** | CTU 伦理委员会批准, 用户同意匿名数据共享 |

---

## 二、数据采集架构

```
[CESNET3 网络流量]
        ↓
   Network TAP (镜像)
        ↓
   监控探针 (200 GB/s 网卡)
        ↓
   Ipfixprobe 流导出器
   (active timeout=5min, inactive timeout=65s)
        ↓
   IPFIXcol2 流收集器
        ↓
   过滤模块（去除穿透流量 + TCP-SYN 扫描）
        ↓
   聚合模块 (10min 窗口 → 时间序列数据点)
        ↓
   TimeScaleDB 数据库
```

**关键过滤步骤：**
- 移除源和目的均不在 CESNET3 的穿透流量
- 过滤掉所有单 TCP-SYN 包（扫描流量）
- 这是 ISP 级数据，不是实验室合成数据

---

## 三、文件结构与层级

### 3.1 目录树

```
CESNET-TimeSeries24/
├── institutions/                    # 机构级 (283 个)
│   ├── agg_10_minutes/
│   │   ├── 1.csv, 2.csv, ...
│   ├── agg_1_hour/                  # ← 本作业使用
│   │   ├── 1.csv, 2.csv, ...
│   ├── agg_1_day/
│   │   ├── 1.csv, 2.csv, ...
│   └── identifiers.csv
│
├── institution_subnets/             # 机构子网级 (548 个)
│   ├── agg_10_minutes/
│   ├── agg_1_hour/
│   ├── agg_1_day/
│   └── identifiers.csv
│
├── ip_addresses_full/               # IP 级 (~275k 个, 40GB)
│   ├── 0/  (0-24999.csv)
│   ├── 1/  (25000-49999.csv)
│   ├── ...
│   ├── agg_10_minutes/
│   ├── agg_1_hour/
│   ├── agg_1_day/
│   └── identifiers.csv
│
├── ip_addresses_sample/             # IP 样例
│   └── ...
│
├── times/                           # 时间戳映射
│   ├── times_10_minutes.csv
│   ├── times_1_hour.csv
│   └── times_1_day.csv
│
├── ids_relationship.csv             # IP ↔ 机构 ↔ 子网 关系表
└── weekends_and_holidays.csv        # 周末/节假日标注
```

### 3.2 三级层级结构

| 层级 | 数量 | 特点 | 本作业的用途 |
|------|------|------|-------------|
| **Institutions（机构级）** | 283 | 流量聚合好, 周期性强, 预测难度低 | **主数据集** |
| **Institution Subnets（子网级）** | 548 | 适中, 包含更多噪声 | 补充实验 |
| **IP Addresses（IP 级）** | ~275k | 稀疏、零值多、预测难度高 | 挑战性实验 |

---

## 四、时间序列指标

### 4.1 基础指标（所有聚合粒度共有）

| 列名 | 中文说明 | 单位/范围 | 类型 |
|------|---------|----------|------|
| `id_time` | 时间窗口 ID | 整数 | 索引 |
| `n_flows` | IP 流数量 | 整数 ≥ 0 | 简单体积 |
| `n_packets` | 包数量 | 整数 ≥ 0 | 简单体积 |
| `n_bytes` | 字节数（传输数据量） | 整数 ≥ 0 | 简单体积 |
| `n_dest_ip` | 唯一目标 IP 数 | 整数 ≥ 0 | 唯一体积 |
| `n_dest_asn` | 唯一目标 ASN 数 | 整数 ≥ 0 | 唯一体积 |
| `n_dest_port` | 唯一目标端口数 | 整数 ≥ 0 | 唯一体积 |
| `tcp_udp_ratio_packets` | TCP/UDP 包比例 | [0, 1] | 比率 |
| `tcp_udp_ratio_bytes` | TCP/UDP 字节比例 | [0, 1] | 比率 |
| `dir_ratio_packets` | 包方向比（出站/总） | [0, 1] | 比率 |
| `dir_ratio_bytes` | 字节方向比（出站/总） | [0, 1] | 比率 |
| `avg_duration` | 平均流持续时间 | 秒 | 平均 |
| `avg_ttl` | 平均 TTL | 整数 | 平均 |

> **本作业重点关注**: `n_bytes`（传输字节数）— 论文中流量预测的主要指标

### 4.2 扩展指标（仅 1 小时 / 1 天聚合）

因为 `n_dest_ip` / `n_dest_asn` / `n_dest_port` 无法通过简单求和从 10min 聚合到 1h，所以扩展了 sum/avg/std：

| 列名 | 说明 |
|------|------|
| `sum_n_dest_ip` | 唯一目标 IP 数之和 |
| `avg_n_dest_ip` | 唯一目标 IP 数均值 |
| `std_n_dest_ip` | 唯一目标 IP 数标准差 |
| `sum_n_dest_asn` | 唯一目标 ASN 数之和 |
| `avg_n_dest_asn` | 唯一目标 ASN 数均值 |
| `std_n_dest_asn` | 唯一目标 ASN 数标准差 |
| `sum_n_dest_port` | 唯一目标端口数之和 |
| `avg_n_dest_port` | 唯一目标端口数均值 |
| `std_n_dest_port` | 唯一目标端口数标准差 |

论文在 1h 聚合级别使用**完整 18 个指标**（13 基础 + 9 扩展去重后 = 18），但训练时**每个指标单独建模（univariate）**。

---

## 五、聚合粒度

论文主实验使用 **1 小时聚合**。三种粒度的选择影响：

| 聚合 | 单序列数据点数 | 缺失率（平均） | 用途 |
|------|---------------|---------------|------|
| 10 分钟 | ~40,320 | ~80%+ | 短时异常检测 |
| **1 小时** | **~6,720** | **~60%** | **流量预测（论文使用）** |
| 1 天 | ~280 | ~40% | 长期趋势 |

> 注意：即便 1 天聚合，平均仍有 40%+ 的缺失窗口。真实 ISP 网络就是有大量间隙。

---

## 六、数据特性

### 6.1 时间序列间隙

- **主要原因**：设备不持续在线（如办公电脑下班关机）
- **分布**：10min 聚合 80%+ 窗口为空，1h 聚合 ~60% 为空
- **工作日模式**：工作日-周末两峰分布，工作日活跃率 60-70%
- **处理建议**：缺失值填充（前向填充 / 零填充）或使用能处理缺失的模型

### 6.2 周期性模式

- **日周期**：明显的白天-夜晚波动
- **周周期**：工作日高、周末低（Fig.5 论文图）
- **学期周期**：大学网络有寒暑假流量下降
- **设备多样性**：办公电脑 / 服务器 / NAT / 蜜罐 / 游戏主机

### 6.3 已知异常事件

| 事件 | 时间 | 表现 |
|------|------|------|
| 某大学网络改造 (Eduroam) | 11 月底 | 活跃 IP 数突增 5 万 |
| 监控探针故障 | 2024-05-21~06-04 | 整体流量骤降 |

### 6.4 异常类型（论文 Fig.9）

- **点异常**：全局离群点、上下文离群点
- **集体异常**：子序列异常、模式异常
- **趋势异常**：趋势漂移 / 概念漂移

---

## 七、本作业使用建议

### 7.1 数据范围

- **使用** `institutions/agg_1_hour/` 下的 283 条序列
- **主要指标**：`n_bytes`（与论文主实验一致）
- **辅助指标**：`n_flows`、`n_packets`（对比验证）

### 7.2 数据分割

按时间顺序（不随机打乱）：
- **训练**：前 70%（约 28 周）
- **验证**：中间 15%（约 6 周） 
- **测试**：后 15%（约 6 周）

### 7.3 缺失值处理

论文处理方法（详见 Fig.3）：
1. 对缺失位置进行标记
2. 使用前向填充或零填充
3. 标准化时忽略缺失位置

### 7.4 滑动窗口设置（Table II）

论文使用的 5 种配置：

| 训练窗口 | 预测窗口 | 含义 |
|---------|---------|------|
| 24h (24) | 24h (24) | 过去 1 天预测未来 1 天 |
| 168h (168) | 24h (24) | 过去 1 周预测未来 1 天 |
| 168h (168) | 168h (168) | 过去 1 周预测未来 1 周 |
| 720h (720) | 24h (24) | 过去 1 月预测未来 1 天 |
| 720h (720) | 168h (168) | 过去 1 月预测未来 1 周 |

---

## 八、配套文件

### ids_relationship.csv

| 列 | 说明 |
|----|------|
| `id_ip` | IP 地址 ID |
| `id_institution` | 所属机构 ID |
| `id_institution_subnet` | 所属机构子网 ID |

### weekends_and_holidays.csv

| 列 | 说明 |
|----|------|
| `Date` | 日期（YYYY-MM-DD） |
| `Type` | `Weekend` 或 `Holiday`（捷克法定假日） |

### times/ 目录

时间窗口序列号 → 绝对时间的映射表，用于画图的 x 轴标签。
