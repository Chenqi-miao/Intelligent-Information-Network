# 期末大作业实施计划 — Comparative Analysis of DL Models for ISP Traffic Forecasting

## 一、总体策略

### 技术栈
- **语言**：纯 Python
- **环境管理**：uv（比 conda 轻量，比 pip 快）
- **深度学习框架**：PyTorch（论文开源代码大概率也是 PyTorch，生态好、调试方便）
- **依赖文件**：`pyproject.toml`（uv 原生支持，比 requirements.txt 更规范）

### 核心原则
- **高效优先**：论文已开源代码，站在前人肩膀上，重点在理解和改进而非从零造轮子
- **聚焦核心**：优先保证 A 类任务（复现）质量，再拓展 B 类任务（对比 + 创新）
- **结果导向**：以最终报告和可运行代码为目标，倒推每日任务

### 执行路径选择
选择 **A 类 + B 类** 全部完成，但有所侧重：
- A 类（基础）：精读 → 数据处理 → 核心模型复现 → 实验验证
- B 类（进阶）：对比实验 + 个人创新点落地

### 项目目录结构（最终目标）
```
final_assigment/
├── README.md                     # 项目说明与运行指南
├── pyproject.toml                # Python 依赖（uv 管理）
├── .python-version               # Python 版本（uv sync 自动识别）
├── data/                         # 数据集（原始+预处理）
│   └── CESNET-TimeSeries24/
├── notebooks/                    # Jupyter 探索/实验
│   ├── 01_data_exploration.ipynb
│   ├── 02_preprocessing.ipynb
│   ├── 03_model_training.ipynb
│   └── 04_analysis.ipynb
├── src/                          # 源代码
│   ├── preprocessing.py          # 数据预处理
│   ├── models.py                 # 模型定义
│   ├── train.py                  # 训练脚本
│   ├── evaluate.py               # 评估脚本
│   ├── utils.py                  # 工具函数
│   └── config.py                 # 配置管理
├── experiments/                  # 实验输出
│   ├── logs/                     # 训练日志
│   ├── checkpoints/              # 模型权重
│   └── results/                  # 结果表格/图表
└── report/                       # 实验报告
    └── final_report.docx
```

---

## 二、时间线（6月17日 — 6月21日）

### Day 1 | 6/17（三）今天 — 环境搭建 + 深度精读 + 数据获取

| 时间 | 任务 | 产出 |
|------|------|------|
| 下午 | **论文精读**：重点读章节 III(数据集)、IV(方法论)、V(实验设置)、VI(结果) | 论文核心要点笔记 |
| 下午 | **uv 环境搭建**：`uv init && uv add torch torchvision torchaudio pandas numpy matplotlib scikit-learn statsmodels` 等 | 可运行的基础环境 |
| 晚上 | **数据获取**：下载 CESNET-TimeSeries24 数据集 | data/ 目录下原始数据 |
| 晚上 | **初始探索**：对数据集做初步 EDA（数据形状、缺失值、分布） | Notebook 01 |

**检查点 ✅**：能加载数据并画出几条时间序列

### Day 2 | 6/18（四）— 数据预处理 + 基线模型

| 时间 | 任务 | 产出 |
|------|------|------|
| 上午 | **数据预处理**：缺失值处理、标准化、训练/验证/测试分割 | preprocessing.py + Notebook 02 |
| 下午 | **Baseline 实现**：Mean 模型 + SARIMA（或用 statsmodels）| models.py 中基线类 |
| 晚上 | **滑动窗口生成**：实现 training window / prediction window 生成逻辑 | utils.py 中窗口函数 |
| 晚上 | **验证基线**：在部分数据上跑基线，确保 pipeline 通顺 | 初步结果 |

**检查点 ✅**：预处理 pipleline 完整，基线模型能出结果

### Day 3 | 6/19（五）— 核心模型复现 + 训练

| 时间 | 任务 | 产出 |
|------|------|------|
| 上午 | **LSTM 模型实现**：参照论文 Table III 超参数，1 层 RNN + 合适 hidden size | models.py 中 LSTM 类 |
| 上午 | **GRU 模型实现**：类似 LSTM 结构 | models.py 中 GRU 类 |
| 下午 | **LSTM-FCN / GRU-FCN**：加上卷积分支的混合架构（选做，时间够就做）| models.py 中 FCN 变体 |
| 下午 | **训练脚本**：train.py — 统一训练循环、日志、checkpoint 保存 | train.py + config.py |
| 晚上 | **启动训练**：在 1-2 条时间序列上训练调参，验证收敛 | logs/ 下训练记录 |
| 晚上 | **扩展训练**：在机构级数据（283 条）上批量训练（可选部分子集）| 训练完成的模型权重 |

**检查点 ✅**：LSTM/GRU 训练能收敛，loss 下降正常

### Day 4 | 6/20（六）— 实验 + 分析 + 个人创新

| 时间 | 任务 | 产出 |
|------|------|------|
| 上午 | **批量评估**：在所有模型上运行 evaluate.py，计算 RMSE/R²/Harmonic Score | results/ 下结果表格 |
| 上午 | **可视化分析**：画对比图（预测曲线对比、误差分布、表格等）| Notebook 03 |
| 下午 | **个人创新 1 — 可解释性**：用 SHAP 分析 LSTM/GRU 模型的特征重要性 | Notebook 04 前半 |
| 下午 | **个人创新 2 — 业务适配思考**：对预测结果做"场景适配性分析"（该模型适合什么类型流量？不适合什么？）| Notebook 04 后半 |
| 晚上 | **结果分析**：与论文结果对比，验证趋势一致性，分析差异原因 | 分析笔记 |
| 晚上 | **统计检验**：论文中有显著性检验，尝试复现 | evaluate.py |

**检查点 ✅**：所有结果表格/图表已生成，分析结论已整理

### Day 5 | 6/21（日）— 报告撰写 + 最终打包

| 时间 | 任务 | 产出 |
|------|------|------|
| 上午 | **报告撰写**：摘要 → 引言 → 方法 → 实验设置 → 结果分析 | report/ 下草稿 |
| 下午 | **报告润色**：完善图表、参考文献、格式调整 | final_report.docx/pdf |
| 下午 | **README 编写**：环境、依赖、数据、运行步骤清晰完整 | README.md |
| 晚上 | **最终检查**：代码可运行确认、文件完整性检查、提交前终审 | 全部提交物 |

**检查点 ✅**：所有提交物就绪，提交截止前完成

---

## 三、详细任务分解

### 3.1 A1 — 精读与理解

**重点阅读章节：**
- **Section I (Introduction)**：研究背景与动机 — 了解为什么流量预测重要
- **Section III (Dataset)**：CESNET-TimeSeries24 数据集详情 — 理解数据来源、层级结构、时间粒度
- **Section IV (Methodology)**：方法论 — 预处理流程、训练/预测窗口设置、模型架构
  - 重点关注：Fig.3 整体方法论流程图、Table II 窗口设置、Table III 超参数
- **Section V (Evaluation)**：评估指标 — RMSE、R²、Harmonic Score 公式和含义
- **Section VI (Results)**：实验结果 — 重点关注 Table IV-VI 和图 5-7

**需要理解的核心概念清单：**
- [ ] 时间序列预测的滑动窗口机制
- [ ] LSTM/GRU 的核心原理（门控机制、记忆单元）
- [ ] 卷积 + 循环的混合架构（FCN 分支）
- [ ] RMSE vs R² vs Harmonic Score 的区别
- [ ] 单变量时间序列预测的流程

### 3.2 A2 — 数据获取与处理

**数据来源：** CESNET-TimeSeries24 数据集
- 论文 GitHub 仓库（论文开源）中应包含数据下载链接或脚本
- 或从 CESNET 官方 / Zenodo 获取

**处理步骤（按 Fig.3 方法论）：**
1. 数据分割：训练/验证/测试（注意按时间顺序，不随机打乱）
2. 标准化：Z-score 标准化（训练集 fit，验证/测试集 transform）
3. 缺失值处理：论文中提到了处理策略（需确认具体方法 — 可能是前向填充或插值）
4. 滑窗生成：将连续时间序列切成 (training_window, prediction_window) 的样本对

**层级选择建议：**
- 优先使用 **机构级（institutions）** 数据（283 条时间序列，相对规整）
- IP 地址级数据含大量零值，预测难度大，可作为挑战性实验

### 3.3 A3 — 算法复现

**必复现模型（优先级高）：**
| 模型 | 复杂度 | 预期工作量 | 论文表现 |
|------|--------|-----------|----------|
| Mean | ⭐ | 10min | 最差基线 |
| SARIMA | ⭐⭐ | 1h | 弱（因不逐序列调参） |
| LSTM | ⭐⭐⭐ | 2h | 优秀 |
| GRU | ⭐⭐⭐ | 2h | 最佳之一 |

**选复现模型（时间充裕时）：**
| 模型 | 复杂度 | 预期工作量 | 论文表现 |
|------|--------|-----------|----------|
| LSTM-FCN | ⭐⭐⭐⭐ | 2h | 优秀 |
| GRU-FCN | ⭐⭐⭐⭐ | 2h | 最佳之一 |
| ETSformer | ⭐⭐⭐⭐⭐ | 3h+ | 中上（简单 transformer） |

**关于论文开源代码：**
- 论文已声明开源，可以下载参考
- **策略：** 先自己根据论文描述实现核心逻辑，再对照开源代码修正
- 不要直接复制 — 理解后自己写一遍才是学习

### 3.4 A4 — 实验验证

**需要复现的关键结果：**
1. Table IV — 各模型的 Overall Mean RMSE
2. Table V — 各模型的 Overall Mean R²-score  
3. Table VI — 各模型的 Harmonic Score
4. Fig.5 — 所有时间序列指标的 Harmonic Score 矩阵
5. Fig.7 — 各模型在所有指标上的整体均值

**预期差异分析：**
- 由于超参数 tuning、随机种子、硬件差异，数值不可能完全一致
- 关键验证点：**模型相对排序趋势是否一致**（GRU/LSTM 优于复杂模型）
- 如果趋势一致，说明复现成功；如果有差异，分析可能原因

### 3.5 B1 — 对比实验

论文本身已经是 10 个模型的对比了，所以 B 类任务的对比实验可以：
1. **补充论文中比较少的指标**：如训练时间、推理时间、参数量
2. **增加传统 ML 基线**：如 Random Forest、XGBoost（论文未涉及）
3. **在不同层级数据上对比**：机构级 vs IP 地址级，分析模型对数据稀疏性的鲁棒性

### 3.6 B2 — 分析报告

从以下角度深入分析（论文中提及但未深入展开的）：
- **模型复杂度 vs 性能**：画参数量-RMSE 散点图
- **训练效率**：各模型训练时间对比（论文 Table VI 隐含了但未明说）
- **零值敏感性**：IP 地址数据含大量零值，分析哪些模型更鲁棒
- **预测窗口影响**：短期（24h）vs 长期（168h）预测，哪个模型更稳定

---

## 四、个人创新落地

### 创新 1：模型可解释性分析
- **工具**：SHAP / LIME
- **内容**：对 LSTM 模型预测结果做 SHAP 值分析
- **产出**：SHAP summary plot，理解模型学到了哪些时间步的模式
- **报告落地**：在报告中增加"模型可解释性分析"小节

### 创新 2：业务场景适配性框架
- **思路**：不把流量预测当作通信专用任务，而是抽象为"通用的时间序列预测业务案例"
- **框架**：建立一个简单的"模型-场景适配矩阵"
  | 场景 | 数据特征 | 推荐模型 | 理由 |
  |------|---------|---------|------|
  | ISP 骨干网 | 强周期性、高聚合 | LSTM/GRU | 论文已验证 |
  | 边缘节点 | 稀疏、零值多 | GRU（更轻量）| 参数量少不易过拟合 |
  | 企业网络 | 工作日/周末模式 | LSTM-FCN | 卷积提取局部特征 |
  | 数据中心 | 突发流量多 | ？ | 可以作为开放问题讨论 |
- **产出**：报告中"模型业务适配性讨论"小节

### 创新 3：工作流改进（MLOps 轻实践）
- **实验跟踪**：用 MLflow 或 TensorBoard 记录每次实验的指标
- **配置管理**：用 YAML/config 文件统一管理超参数，不硬编码
- **可重复性**：固定随机种子，记录实验环境信息
- **产出**：README 中说明，并在实验脚本中体现

> **注意：** 创新是为报告和答辩增色的，不要因为创新挤占了基础复现的时间。优先级永远是：A 类完成 > B 类完成 > 创新落地。

---

## 五、关键技术参考

### CESNET-TimeSeries24 数据集关键信息
- 时间跨度：2023 年 10 月 — 2024 年 7 月（40 周）
- 聚合粒度：1 小时（论文主要使用这个级别）
- 层级：
  - 283 条机构级
  - 548 条机构子网级
  - 1000 条 IP 地址级
- 可用时间序列指标（Table I）：Number of bytes, Number of packets, Number of flows, 等共 18 个

### 模型配置参考（Table III — 最终超参数）
- RNN 层数：大多数为 1 层
- Hidden size：需查看论文具体数值（通常在 20-200 范围）
- 学习率、batch size：具体值需查阅论文

### 训练/预测窗口（Table II）
- (24, 24) — 用过去 24h 预测未来 24h（短期）
- (168, 24) — 用过去一周预测未来 24h
- (168, 168) — 用过去一周预测未来一周（长期）
- (720, 24) — 用过去一个月预测未来 24h
- (720, 168) — 用过去一个月预测未来一周

### 评估指标公式
- **RMSE**：√(1/N ∑(yᵢ - ŷᵢ)²)
- **R²**：1 - ∑(yᵢ-ŷᵢ)² / ∑(yᵢ-μᵧ)²
- **Harmonic Score**：结合多个指标的综合评分（论文详细公式）

---

## 六、风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|---------|
| 数据集下载慢/找不到 | 低 | 高 | 提前下载，论文 GitHub 仓库有链接 |
| 模型训练时间过长 | 中 | 中 | 先在小子集上调通，再扩展；使用 GPU/Colab |
| 论文关键细节缺失 | 中 | 中 | 参考论文开源代码补充 |
| 环境依赖冲突 | 低 | 中 | 使用 uv 虚拟环境 + `uv lock` 固定版本 |
| 时间不够 | 中 | 高 | 按优先级砍任务：优先保证 4 个核心模型 |

### 时间不够时的妥协方案
- **如果 Day 3 结束时 LSTM/GRU 还没跑通**：跳过 FCN 变体，用 PyTorch Lightning/TensorFlow 快速实现
- **如果 Day 4 训练来不及完成**：只在 10-20 条时间序列上训练（论文用了 283+条），说明"在小规模子集上验证趋势"
- **如果 Day 5 报告写不完**：先完成结果部分，方法和实验设置可以复用论文描述

---

## 七、每日检查清单

### Day 1 结束时
- [ ] Python 虚拟环境创建成功
- [ ] 论文精读完成，关键图表已标注
- [ ] 数据集已下载，能成功加载
- [ ] EDA Notebook 完成

### Day 2 结束时
- [ ] 预处理 pipeline 完整（缺失值 → 标准化 → 滑窗）
- [ ] Mean 基线模型能跑出结果
- [ ] SARIMA 基线模型能跑出结果
- [ ] 数据分割逻辑验证正确

### Day 3 结束时
- [ ] LSTM 模型训练能收敛
- [ ] GRU 模型训练能收敛
- [ ] checkpoint 保存正常
- [ ] 至少在 1 个窗口配置上完成了训练

### Day 4 结束时
- [ ] 所有模型评估完成
- [ ] 结果图表已生成
- [ ] 与论文结果的对比分析完成
- [ ] 个人创新内容初步完成

### Day 5 最终提交前
- [ ] 报告完整（摘要 → 参考文献）
- [ ] README.md 完整
- [ ] 代码无语法错误
- [ ] requirements.txt 完整
- [ ] 所有文件打包
