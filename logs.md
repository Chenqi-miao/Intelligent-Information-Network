# 项目日志

## Day 1 (6/17) — 环境搭建 + 数据准备 + EDA

### CLAUDE.md 与协作模式
添加 CLAUDE.md 后协作效率明显提升，尤其是"指导边界"规则让 AI 不乱改代码，每一步都可控。
但发现 CLAUDE.md 不能跨 AI 通用——不同 agent 需要不同的"灵魂"，用同一套规范效果差很多。

### 多 Agent 协作的教训
同时用了 3 个 AI（NotebookLM 记笔记/读论文、Hermes 下载数据、Claude 写代码），但协作困难：
- 流程做不到自动化，每一步需要人工盯着
- AI 对网络问题不敏感，常把下载失败误判为代码错误，反复返工
- Minimax 代码能力弱，改几遍最后还是手动执行

### 数据集下载
Zenodo 直接下载，但 institution_subnets.tar.gz 下了两次都不完整（第一次 279MB，第二次 628MB，预期 774MB），最后保留第一次解压的 546/548 条。

### 论文阅读
数据集论文没写明具体用了哪部分数据，需要根据预测论文的 method/result 反推（283 机构 + 548 子网 + 1000 IP）。

### EDA 核心发现
做了 5 步分析：层级对比 → 周期性 → 缺失率 → 多指标相关性 → 数据分布
- 机构级缺失率 ~1%，基本不用处理；IP 级 ~79%，需要特殊策略
- n_bytes / n_flows / n_packets 高度相关（r > 0.9），选一个就够了
- 比率指标（tcp_udp_ratio, dir_ratio）独立性强，是多变量预测的切入点
- 日志显示 24h + 168h 双周期，验证了论文窗口选择

### 教训
论文描述和实际 CSV 格式有偏差（如缺失值的定义），与其看论文猜，不如直接读数据。
